-
  !python {model: ir.module.module}: |
    lang_code = 'es_ES'
    # lang_code = 'es_AR'
    module_ids = self.search(cr, uid, [('state', '=', 'installed')])
    self.update_translations(cr, uid, module_ids, lang_code, {})
    user_ids = self.pool['res.users'].search(cr, uid, [])
    self.pool['res.users'].search(cr, uid, user_ids, {'lang': lang_code})
