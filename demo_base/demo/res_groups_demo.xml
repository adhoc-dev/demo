<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- grupos que tipicamente se configuran desde config settings para activar opciones  -->
    <record id="base.group_user" model="res.groups">
        <field name="implied_ids" eval="[(4, ref('account.group_delivery_invoice_address')),
                                         (4, ref('crm.group_use_lead')),
                                         (4, ref('product.group_discount_per_so_line')),
                                         (4, ref('product.group_product_pricelist')),
                                         (4, ref('product.group_product_variant')),
                                         (3, ref('purchase.group_send_reminder'))]"/>
    </record>

</odoo>
